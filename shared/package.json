{
  "name": "@argus/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and contracts between Argus frontend and backend.",
  "main": "api-types.ts",
  "scripts": {
    "typecheck": "tsc --noEmit api-types.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
